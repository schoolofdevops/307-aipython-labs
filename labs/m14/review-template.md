# Diff Review — check-deps --format json

Review the diff in `labs/m14/rigged-diff.patch`. For each flaw you find, fill in one row below.
The diff contains **six** seeded flaws across these categories:

- **missing-timeout** — an HTTP call with no timeout set
- **leaked-credential** — a credential value appears in a log line or output
- **swallowed-exception** — an exception is caught and silently discarded
- **hallucinated-api** — an import or call to a module/function that does not exist
- **needless-dependency** — a new dependency added that the feature does not need
- **destructive-default** — an action that deletes or destroys data without confirmation

## Findings

| # | File | Line(s) | Category | What is wrong | Fix instruction |
|---|------|---------|----------|---------------|-----------------|
| 1 |      |         |          |               |                 |
| 2 |      |         |          |               |                 |
| 3 |      |         |          |               |                 |
| 4 |      |         |          |               |                 |
| 5 |      |         |          |               |                 |
| 6 |      |         |          |               |                 |
